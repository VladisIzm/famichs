<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Telegram Web App with TON Connect</title>
    <script src="https://unpkg.com/@tonconnect/ui@latest/dist/tonconnect-ui.min.js"></script>
    <style>
        body {
            margin: 0;
            font-family: Arial, sans-serif;
            text-align: center;
            background-color: #f4f4f4;
        }
        .loading {
            display: flex;
            align-items: center;
            justify-content: center;
            height: 100vh;
            background-color: #000;
            color: #fff;
            font-size: 24px;
        }
        .app {
            display: none;
            padding: 20px;
        }
        .photo {
            width: 100%;
            height: 40vh;
            background-image: url('https://via.placeholder.com/600x400');
            background-size: cover;
            background-position: center;
        }
        .balance {
            font-size: 32px;
            margin: 20px 0;
        }
        .menu {
            position: fixed;
            bottom: 0;
            width: 100%;
            display: flex;
            justify-content: space-around;
            background-color: #fff;
            border-top: 1px solid #ddd;
            padding: 10px 0;
        }
        .menu button {
            background: none;
            border: none;
            font-size: 18px;
            cursor: pointer;
        }
        .wallet-page {
            display: none;
        }
        .wallet-page.active {
            display: block;
        }
    </style>
</head>
<body>

<div class="loading">Анализ данных...</div>

<div class="app">
    <div class="photo"></div>
    <div class="balance">Ваш баланс: <span id="balance">100</span> монеток</div>

    <div class="wallet-page">
        <h2>Баланс: <span id="wallet-balance">100</span> монеток</h2>
        <p>Зачисление: <span id="deposit">50</span> монеток</p>
        <button id="link-wallet">Привязать криптокошелек</button>
    </div>

    <div class="menu">
        <button id="home-btn">Home</button>
        <button id="wallet-btn">Wallet</button>
    </div>
</div>

<script>
    const tonConnectUI = new TON_CONNECT_UI.TonConnectUI({
        manifestUrl: 'https://your-app-url.com/tonconnect-manifest.json',
        buttonRootId: 'ton-connect'
    });

    const isFirstVisit = !localStorage.getItem('visited');
    const balance = parseInt(localStorage.getItem('balance')) || 100; // Начальный баланс

    if (isFirstVisit) {
        setTimeout(() => {
            document.querySelector('.loading').style.display = 'none';
            document.querySelector('.app').style.display = 'block';
            localStorage.setItem('visited', 'true');
            localStorage.setItem('balance', balance.toString());
            document.getElementById('balance').textContent = balance;
        }, 2000);
    } else {
        document.querySelector('.loading').style.display = 'none';
        document.querySelector('.app').style.display = 'block';
        document.getElementById('balance').textContent = balance;
    }

    document.getElementById('home-btn').addEventListener('click', () => {
        document.querySelector('.wallet-page').classList.remove('active');
    });

    document.getElementById('wallet-btn').addEventListener('click', () => {
        document.querySelector('.wallet-page').classList.add('active');
        document.getElementById('wallet-balance').textContent = balance;
        document.getElementById('deposit').textContent = 50; // Пример зачисления
    });

    document.getElementById('link-wallet').addEventListener('click', async () => {
        try {
            const connectedWallet = await tonConnectUI.connectWallet();
            console.log('Кошелек подключен:', connectedWallet);
            if (connectedWallet) {
                document.getElementById('link-wallet').textContent = connectedWallet.account.address;
            }
        } catch (error) {
            console.error("Ошибка при подключении кошелька:", error);
        }
    });

    tonConnectUI.onStatusChange((wallet) => {
        if (wallet) {
            document.getElementById('link-wallet').textContent = wallet.account.address;
        }
    });

    tonConnectUI.ready();
</script>

</body>
</html>
